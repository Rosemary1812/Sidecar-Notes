{
  "name": "sidecar-notes",
  "version": "1.0.0",
  "description": "A sidecar note workflow with split view, synced quotes, and companion note files.",
  "main": "main.js",
  "scripts": {
    "build": "tsc -noEmit && node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --dev"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "sidecar",
    "annotation",
    "notes"
  ],
  "author": "Sidecar Notes contributors",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "^1.12.3",
    "typescript": "^5.4.0"
  }
}
